# Claude + AI Digest — 2026-08-02

**Nothing new since yesterday in Claude/Claude Code releases** *(New capability, high confidence — primary source, CHANGELOG.md)*. Newest entry is still v2.1.220 ("bug fixes and reliability improvements"), unchanged for several days running; v2.1.219 (Opus 5 default, sandbox network allowlist, nested subagent depth-3, etc.) remains the last real feature entry and was already covered in earlier digests. A site:anthropic.com sweep and a YouTube feature-demo search both turned up nothing dated inside the lookback window — the newsroom hits this run (a rare-disease research grant, an open-weights policy position, an expanded Cognizant enterprise partnership) are business/policy items outside this category's scope, and the latter two are also both dated July 27 anyway, outside the window.

**A "read the Claude Code source" writeup surfaced three undocumented configuration hooks worth knowing** *(New technique, medium confidence — found via a search-engine summary of buildingbetter.tech's "I Read the Claude Code Source Code" post; the article itself 403'd on direct fetch, so treat specifics as reported-not-verified until you skim it yourself)*. Three findings: (1) skill frontmatter supports `disable-model-invocation: true`, which stops Claude from auto-triggering a skill on its own judgment — only an explicit `/skill-name` invokes it, meant for skills with destructive/irreversible steps; (2) custom subagent frontmatter (`.claude/agents/*.md`) supports a `memory` field giving that agent persistent state across separate invocations, not just within one session — e.g. a reviewer subagent that remembers past findings; (3) hooks can return structured JSON on stdout (not just an exit code) with event-specific fields that alter behavior in real time, beyond the documented exit-code-2-blocks pattern. Tie-in: all three are skill/subagent/hook authoring levers — exactly the layer ponytail mode, Council-loop's own doctor/repair tooling, and this digest routine itself live in. `disable-model-invocation` is a good fit for anything security- or safety-adjacent (a skill that could touch NEXUS's broker or vault, say), and the `memory` field could let a Council-loop review role carry state across runs instead of starting cold each time.

**No verifiably notable new Claude buzz this run** *(Claude buzz)*. Nothing surfaced beyond the shared-chats-indexed-by-Google privacy story, which is already reported (2026-07-27 digest) and was resolved by Anthropic within days.

**MiniMax shipped H3, an omni-modal video model generating 15-second 2K clips with native synchronized stereo audio, with open weights promised** *(Other AI, high confidence — corroborated by MarkTechPost, multiple AI-news outlets, and early creator posts using preview access)*. Released July 30-31, this is a step up from typical silent or short AI video generators — audio is generated in the same pass as video, not a separate dubbing step, at full 2K resolution. Worth tracking as a genuinely open-weight release in a space (video+audio generation) still mostly closed/API-only at the frontier; no direct tie-in to Brian's current projects, just an industry marker.
