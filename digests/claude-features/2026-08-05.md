# Claude + AI Digest — 2026-08-05

## 1. Claude Opus 4.1 retires from the API today [New capability]
Anthropic's newsroom/API release notes show `claude-opus-4-1-20250805` is deprecated with retirement on the Claude API landing **today, Aug 5 2026** — Anthropic is pointing everyone to Opus 4.8. Found via a site-scoped anthropic.com search (moderate confidence — didn't fetch the full release-notes page directly). Worth a five-minute grep across NEXUS's `.env`/config and Council-loop's `.council/config.local.json` for any lingering hardcoded `claude-opus-4-1-*` string, since both already default to newer Opus/Sonnet tiers per this repo's CLAUDE.md but a stray override elsewhere would now start failing outright instead of degrading.

## 2. Mid-conversation system messages land on Fable 5 / Mythos 5 / Opus 4.8 [New capability]
Anthropic's API release notes list a new capability: system messages can now be injected mid-conversation (not just at the start) for Claude Fable 5, Claude Mythos 5, and Claude Opus 4.8, across the Claude API, Bedrock, and GCP. Found via site-scoped anthropic.com search — moderate confidence, didn't verify against the primary docs page directly. Could matter for NEXUS's `router.py` tool-use loop or the orchestrator's plan/execute split if a future need arises to inject fresh constraints (e.g. an updated budget/kill-switch state) partway through a long tool-calling round instead of only at prompt-build time.

## 3. Claude Code Desktop in-app browser with click-to-annotate [New capability]
Search coverage (aggregator/changelog summaries, not Anthropic's own page directly — lower confidence, flagging that explicitly) describes a Desktop-only in-app browser: Claude can open local dev-server previews or docs, and you can select a page element, circle/annotate the area you want changed, and drop that screenshot straight into the chat. Not independently confirmed against a primary source this run. If real, it's a tighter loop than manual screenshot-and-paste for NEXUS frontend work (`frontend/` pages) or GLP APP's single-file HTML UI tweaks.

## 4. "Away summary" on returning to a paused session [New capability]
Same secondary-source coverage as #3 (unverified against a primary changelog this run) describes a Claude Code feature that auto-generates a recap of what happened while you were away from a paused session, on by default. Low-to-moderate confidence. Would mostly matter for long-running Council-loop or Workflow-tool sessions Brian steps away from mid-run — a built-in "what did I miss" beats scrolling transcript.

## 5. Hierarchical/nested subagent spawning (up to 3 levels deep) [New technique]
Coverage from June 2026 (older than the usual lookback window, but genuinely new-to-Brian as a technique, so included under category 2 rather than 1) describes Claude Code letting a parent agent spawn child agents that can themselves spawn further child agents, up to three levels. Distinct from what's documented here: this session's own Agent tool is flat (subagents can't spawn subagents), and the Workflow tool explicitly caps `workflow()` nesting at one level. If accurate, worth testing carefully before leaning on it — deep agent trees multiply token spend fast, and Nexus/Council-loop's cost-governor-style discipline doesn't have an equivalent in a bare nested-agent tree.

## 6. Claude social buzz: nothing verifiably notable this cycle [Claude buzz]
No viral demo, large-reach video, or notable controversy surfaced with confirmable reach (view counts, engagement, or secondary coverage) in this run's searches. Skipping rather than padding with a minor mention.

## 7. OpenAI GPT-5.6 GA; xAI shipping voice + Grok 4.6 this week [Other AI]
OpenAI's GPT-5.6 family (Sol/Terra/Luna) reached general availability July 9 after a gated preview. More immediately: xAI's `grok-voice-think-fast-2.0` speech-to-speech model is rolling out starting today (Aug 5), and Grok 4.6 is expected Aug 7 with specs/pricing still undisclosed as of this run. Found via general industry-news search, not primary xAI/OpenAI newsroom pages (that site-scoped query came back empty of anything current) — treat the Grok 4.6 date as a rumor/expectation, not confirmed. No direct tie-in to Brian's projects; flagged for industry awareness only.
