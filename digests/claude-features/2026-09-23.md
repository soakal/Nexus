# Claude + AI Digest — 2026-09-23

## 1. Claude Opus 5.5 is now the default Opus model in Claude Code [New capability]
Claude Code v2.1.280 (Sept 22) made Claude Opus 5.5 (`claude-opus-5-5`) the new default Opus model — 1M-token context window, adaptive thinking always on, and pricing actually dropped versus Opus 4.8 ($4/$20 per Mtok in/out, down from $5/$25). Confirmed via the official CHANGELOG.md, cross-checked against platform.claude.com's model docs and independent coverage (high confidence). This session's own fallback chain already lists `claude-opus-5-5[1m]`, so it's not hypothetical — it's live infra you're one config nudge away from using directly.
Tie-in: NEXUS's `_PRICE_PER_MTOK` table in `router.py` carries a comment that pricing needs periodic re-verification — this is a concrete trigger to add/confirm a real `claude-opus-5-5` entry (with the correct $4/$20 rate) rather than letting it fall through to an unpriced-model $0 default the next time `orchestrator_planner_model`/`orchestrator_verifier_model` get bumped up from the balanced-profile default.

## 2. Claude Code 2.1.280 added a web admin UI for Routines [New capability]
The same v2.1.280 release added "web-based Claude admin settings for Routines" per the official changelog — a dedicated management surface for scheduled cloud routines (the exact mechanism this digest runs under). No deeper detail was fetched beyond the changelog bullet itself, so treat the specifics (what's configurable, whether it covers non-admin accounts) as unconfirmed until you look.
Tie-in: Worth a glance next time you're auditing this digest's own routine or any other scheduled Claude Code work — could be a cleaner place to review/adjust routine config than wherever you're doing it today.

## 3. New-to-Brian techniques: nothing surfaced this cycle [New technique]
Simon Willison's site has no new Claude Code post since the already-known Jul 19 Bun/Rust entry. Hacker News's Claude Code search turned up nothing beyond previously-seen threads. Neither subreddit was reachable via site-scoped search (the same recurring indexing gap noted in prior digests). Genuinely quiet cycle for non-obvious technique content.

## 4. Claude buzz: nothing verifiably notable this cycle [Claude buzz]
No confirmable viral demo, controversy, or major reaction piece turned up beyond the Opus 5.5 rollout itself (covered as a feature in item 1, not buzz). Not padding this with a minor mention.

## 5. OpenAI ships GPT-6 Sol and Luna, cheaper GPT-6-family siblings [Other AI]
OpenAI released GPT-6 Sol and GPT-6 Luna on Sept 22 — smaller, faster/cheaper models extending the GPT-6 Astra family launched earlier this month. Sol adds more reasoning capability at $2/$10 per Mtok in/out; Luna targets high-volume/fast-response use at $0.10/$0.50 — both roughly 50% cheaper than their GPT-5.6 equivalents, with OpenAI claiming about half the mistake rate of GPT-5.6 Sol on its own benchmarks. Corroborated across OpenAI's own announcement page plus independent coverage (TechCrunch, MacRumors, The New Stack, gHacks) — high confidence on specs and pricing.
No forced tie-in — a pure industry pricing/quality data point, useful context if you're ever weighing OpenRouter model options.
