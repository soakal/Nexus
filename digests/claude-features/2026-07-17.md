# Claude + AI Digest — 2026-07-17

**Claude Code 2.1.212 shipped** *(New capability, high confidence)*. Per the official raw CHANGELOG (primary source), `/fork` was redesigned so a conversation copies into a background session while your current one keeps working, with a new `/subtask` command replacing the old in-session subagent launcher. The same release adds session-wide runaway guards (WebSearch capped at 200 calls/session, subagent spawns capped at 200/session), auto-backgrounds any MCP tool call that runs over 2 minutes, gives `/resume` a picker that can reach past *and deleted* sessions, and lands several security fixes around plan mode, worktree creation, and hook infrastructure. Tie-in: the subagent-spawn cap is worth knowing before a Workflow-tool script or Council Loop cycle fans out aggressively enough to hit it; the deleted-session `/resume` picker could recover a session Brian thought was gone.

**Claude Sonnet 5 has introductory pricing — $2/M input, $10/M output tokens through August 31, 2026** *(New capability, medium-high confidence — from an anthropic.com search summary, not independently page-fetched this run)*, alongside increased rate limits across Chat, Cowork, Claude Code, and the API. Tie-in: this is directly actionable for NEXUS — `backend/agents/router.py::_PRICE_PER_MTOK` carries a `# VERIFY against current Anthropic pricing` placeholder for exactly this, and the cost governor's dollar figures (daily/per-task caps, spend reports) are only as good as that table; worth updating it before the promo lapses on 8/31 skews the numbers back up.

**Category 2 (new-to-Brian techniques): nothing survived the filter this run.** Simon Willison and Hacker News searches surfaced only older or already-known material (past version retrospectives, the token-overhead story already covered 2026-07-14) — nothing genuinely non-obvious against the preferred sources today.

**Category 3 (Claude social/video buzz): nothing verifiable this run.** The video search only turned up older (March–June) "new features explained" style uploads, and a reddit `site:` query returned no results — no fresh, verifiably large Claude-specific viral moment surfaced in the window.

**Gemini 3.5 Pro has now missed its third consecutive launch target, including today's July 17 date** *(Other AI, well-corroborated across secondary tech outlets — no Google/DeepMind primary confirmation, since Google hasn't announced a date at all)*. Multiple trade outlets report Google scrapped an earlier build after finding structural failures in recursive tool-calling and SVG generation, rebuilt it with a reported 2M-token context window and a "Deep Think" reasoning layer, and is now still fighting hallucination/consistency issues that are keeping it from shipping — prediction markets have reportedly shifted expectations to August. Notable mainly as a story about a heavily-hyped frontier model repeatedly failing to ship, not a launch to react to.

**Kimi K3, a new frontier model from Moonshot AI, reportedly released July 16** *(Other AI, low-medium confidence — surfaced only via one tracker aggregator, not corroborated elsewhere this run)*. No further detail was reachable within this run's query budget; flagging only as a name worth recognizing if it comes up, not a verified capability claim.
