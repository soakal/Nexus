# Claude + AI Digest — 2026-09-05

*(Note: last digest ran 2026-08-31; this run's lookback was widened to ~7 days to cover the gap.)*

## 1. Claude Fable 5.1 and a new restricted "Mythos" tier launched Sep 1 [New capability]
Anthropic shipped Claude Fable 5.1 (generally available, replacing the Fable 5 already in Brian's lineup) on September 1, 2026. Alongside it, Anthropic introduced "Claude Mythos" — per Anthropic's own `claude/mythos` page and a red-team writeup at `red.anthropic.com`, corroborated by several secondary write-ups (Pluralsight, MindStudio) — described as the *same underlying model as Fable 5.1 but with reduced safeguards*, available only through trusted-access programs for cybersecurity/life-sciences work. Confidence caveat: Anthropic's own pages were blocked by this session's egress proxy, so this is drawn from search-result summaries and secondary sources, not a direct read — worth a direct look before treating specifics (e.g. the 83%-first-attempt-exploit claim) as verified. No direct NEXUS tie-in (Mythos access is gated), but the Fable 5.1 GA swap is a plain drop-in model bump wherever Fable 5 is configured today.

## 2. Claude Code CLI: `/skill-doctor`, 128K inline output, fullscreen `/diff` [New capability]
Five CLI versions shipped since the last digest (2.1.257→2.1.261, all new since the 08-31 edition's 2.1.251 cutoff), via the GitHub changelog (high confidence, primary source). Highlights: `/skill-doctor` — a new command that flags *unused* skills and reports the context-token cost of each installed skill; `bashOutputMaxChars`/`taskOutputMaxChars` settings raising inline output display up to 128K (was much lower); a fullscreen `/diff` panel for uncommitted changes; and `/cost` now surfaces prompt-cache-miss diagnostics. Tie-in: `/skill-doctor` is a direct fit given how many skills are loaded in this environment alone (see this session's own skill listing) — worth running once to see which are dead weight and how much context budget each one costs before every turn.

## 3. Subagent `color` frontmatter field for visually distinguishing parallel agents [New technique]
Lower-confidence, minor finding — a search-engine summary of the same buildingbetter.tech Claude Code source-code deep-dive this digest has partially mined before (its `disable-model-invocation`/hooks-stdout-JSON/`memory` findings are already covered in past editions, not re-flagged here) surfaced one previously-unreported detail: subagent frontmatter supports a `color` field (red/orange/yellow/green/blue/purple/pink/gray) purely to visually distinguish agents when several run at once. Modest value, but genuinely new-to-Brian and could help at a glance when running several Agent-tool calls or a Workflow in parallel, since nothing else in the UI currently labels which pane is which subagent.

## 4. Claude buzz: nothing verifiable this cycle [Claude buzz]
The subreddit site-search (r/ClaudeAI / r/ClaudeCode) again returned no actual subreddit content — the same standing tooling gap noted in prior editions. The general buzz sweep surfaced only a "Claude Code 3.0" YouTube video titled as posted "5 days ago" with no view-count or secondary-coverage signal to confirm real reach, so it isn't reported as a finding — title only, unconfirmed content or virality.

## 5. Four labs shipped new models within ~72 hours of each other (Sept 1–2) [Other AI]
Per an aggregator sweep (llm-stats.com / llmgateway.io style trackers, medium confidence — not each lab's own primary announcement): Google's Gemini 3.8 Flash, Meta's Muse Spark 1.3, and a new OpenAI model reported as "OpenAI Astra" all landed within the same ~72-hour window as Anthropic's Fable 5.1 above. Separately, DeepSeek's V4-Pro (a 1.6T-parameter MoE, ~49B active params/token, 1M-token context) is now cited as part of the current frontier set. Worth knowing the pace of releases is compressing industry-wide right now; nothing here changes any of Brian's own tooling directly, but "OpenAI Astra" in particular wasn't corroborated against OpenAI's own newsroom in this pass and should be treated as unverified by name until seen there directly.
