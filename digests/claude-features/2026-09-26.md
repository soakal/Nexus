# Claude + AI Digest — 2026-09-26

## 1. Claude Code 2.1.283: `/doctor prompt-audit`, model-pinning settings, stdio MCP session-leak fix [New capability]
Shipped since yesterday's digest (which covered 2.1.282). New: `/doctor prompt-audit`, a command that scans CLAUDE.md files and prompts for outdated model-name patterns; `availableModelsMatch`/`deniedModels` managed settings for pinning or blocking specific model versions; MCP tool outputs now flow into OpenTelemetry span events; and an `x-claude-code-prompt-id` gateway header for grouping requests. Fixes: stdio MCP servers staying alive after a session ends, MCP progress notifications getting dropped during background-task transitions, SDK sessions losing deferred tool calls when a turn ends early, and the `/model` picker accepting malformed Sonnet-version suffixes. Confirmed via the official GitHub CHANGELOG.md (primary source, high confidence).
Tie-in: `/doctor prompt-audit` is worth pointing at this very CLAUDE.md — it's a large, actively-maintained living document that already tracks model names by hand (see the "Model pipeline" section's promo-rate expiry note), and a built-in scanner could catch a stale model reference nobody manually rechecked after the next update. The stdio-MCP-server-leak fix is also relevant to MCP_Obsidian or the Chrome-automation MCP server if either has ever seemed to linger past a session closing.

## 2. New-to-Brian techniques: nothing surfaced this cycle [New technique]
Simon Willison's site has no new Claude Code post since the already-known Jul 19 Bun/Rust entry. A Hacker News search returned only old/evergreen threads with no reliable date signal (top hits included the "Claude Code 2.0" launch thread and general retrospectives), and a September-2026-scoped variant returned aggregator/newsletter sites rather than real HN threads. Both r/ClaudeAI and r/ClaudeCode remained unreachable via site-scoped search — the same recurring indexing gap flagged in prior digests. Genuinely quiet cycle for non-obvious technique content.

## 3. "Artificial Gooner Intelligence": a Claude-in-Chrome debugging session allegedly lands on an adult site, goes viral [Claude buzz]
A user's Claude-in-Chrome autonomous debugging session reportedly wandered onto an explicit website mid-task; screenshots/recordings spread across X (including a Polymarket repost with high engagement) and secondary tech-culture coverage (LatestLY, StackUmbrella), spawning "Artificial Gooner Intelligence" memes riffing on AGI. Real reach — multiple independent viral posts plus secondary write-ups — but the underlying incident itself has NOT been independently verified: no named developer, no reproducible repro, no Anthropic statement confirming it happened as described. Treat the meme's reach as confirmed and the incident's authenticity as unconfirmed.
Tie-in: a cautionary illustration for your own Chrome-automation MCP server — coverage notes Anthropic's own extension blocks adult sites by default, but this is exactly the "agent follows an unintended link trail during autonomous browsing" failure mode worth keeping in mind if that server is ever given a broader browsing scope.

## 4. Other AI: nothing new this cycle beyond already-covered ground [Other AI]
Grok 4.7 (xAI, Sept 21) and GPT-6 Sol (OpenAI, Sept 22) were already reported in the 2026-09-22 and 2026-09-23 digests — not re-surfaced here. Site-scoped OpenAI/DeepMind newsroom searches showed nothing freshly dated inside this 48h window, and a Meta/Mistral/DeepSeek sweep found no release specifically dated Sept 24-26 — only generic aggregator noise (LLM Gateway's tracker cites 23 new models released across all of September, with nothing attributable to this window).
