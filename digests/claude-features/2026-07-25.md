# Claude + AI Digest — 2026-07-25

**Claude Opus 5 launched, now the default model on Claude Max and strongest on Pro** *(New capability, high confidence — primary source anthropic.com/news/claude-opus-5, corroborated by the official Claude Code CHANGELOG.md v2.1.219)*. Released ~July 24; per Anthropic, it's SOTA on coding/knowledge-work evals (Frontier-Bench, GDPval-AA) while staying behind Mythos 5 on cybersecurity tasks, at the same price as its predecessor Opus 4.8. The changelog entry adds specifics: 1M-token context window, $10/$50 per Mtok API pricing. This supersedes the "Opus 4.8 is latest" line in your own model-lineup notes — worth updating if `orchestrator_planner_model`/`orchestrator_verifier_model` in NEXUS still assume 4.8 as the "restore max quality" fallback.

**Claude Code v2.1.219 bumped subagent nesting depth from 1 to 3, plus a new `sandbox.network.strictAllowlist` setting and a `DirectoryAdded` hook** *(New capability, high confidence — official CHANGELOG.md)*. Depth-3 nesting means a subagent can now spawn its own subagents two levels deeper than before. v2.1.218 separately converted `/code-review` to a background subagent so it no longer fills the main conversation. Tie-in: deeper nesting is directly relevant to how you already use the Agent/Workflow tools and Council Loop's multi-role structure — worth a quick check of whether any existing agent design assumed the old depth-1 ceiling as a hard limit.

**Simon Willison documented deploying Claude Code's cloud-created branches straight to GitHub Pages for instant shareable previews** *(New technique, high confidence — primary source til.simonwillison.net, corroborated by a Hacker News discussion)*. Point GitHub Pages at the `claude/...` branch Claude Code for web creates, and self-contained HTML/vanilla-JS builds (CDN-loaded deps, no build step) become live at a `github.io` URL within about a minute — with none of the CSP restrictions that limit Claude.ai Artifacts, so pages can hit external JSON APIs directly. Tie-in: relevant to single-file tools like GLP APP, or for quick shareable previews of Shantry Bills/CWI AI frontend tweaks done via Claude Code on the web instead of locally.

**A third-party deep-dive claims to have found undocumented Claude Code features by reading its shipped source in node_modules** *(New technique, LOW confidence — single source, buildingbetter.tech, unconfirmed by Anthropic docs)*. Claimed finds: a "memories/dreams" consolidation system where sessions produce memories that get distilled into longer-term ones informing future sessions, and a "MAGIC DOC:" H1-heading convention that scopes an auto-doc-update agent to only touch the section beneath it. Take with real skepticism since it's unverified — but if real, the MAGIC DOC convention could be worth testing against your CLAUDE.md file-based memory system to scope automated doc updates more tightly.

**No verifiably notable Claude buzz this cycle** *(Claude buzz)* — searches surfaced a cluster of low-quality aggregator sites (IBTimes UK, evrimagaci.org and similar) pushing an unverified, serious claim about military AI misuse that no credible outlet corroborated; not repeating the specific claim here since it doesn't clear the sourcing bar and the topic is sensitive. Legitimate Opus 5 launch coverage exists but is too fresh (released ~18-24h ago) to show real view-count/engagement signal yet — nothing to report as confirmed virality today.

**No major other-AI development this cycle** *(Other AI)* — the sweep of lab newsrooms and broader industry trackers turned up only routine incremental releases (Gemini 3.6 Flash / 3.5 Flash-Lite on July 21, Grok STT 1.0 on July 23), not a headline-grade model launch, benchmark result, or controversy. Nothing cleared the "significant, not routine" bar today.
