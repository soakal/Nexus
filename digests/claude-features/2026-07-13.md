# Claude + AI Digest — 2026-07-13

**Claude Code's 50% weekly-limit boost got extended to July 19** *(New capability)*. The promotion that raised weekly usage limits by 50% for Pro/Max/Team/seat-based-Enterprise plans (running since May 13) was due to expire today, July 13, but Anthropic extended it another week. No action needed — it applies automatically across CLI, IDE, desktop, and web. Confidence: high, corroborated by Help Net Security's same-day (Jul 13) coverage plus several independent trackers. Tie-in: worth knowing purely as a heads-up, since Brian runs several autonomous loops (this digest, Council Loop, SOPForge) that chew through Claude Code usage — a few extra days of headroom before limits snap back on the 19th.

**`/doctor` now flags bloated checked-in CLAUDE.md files** *(New capability)*. Per the official CLI changelog (v2.1.206), `/doctor` gained a check that proposes trimming a repo's CLAUDE.md by cutting content Claude could just derive from the codebase itself. Confidence: high — primary source (Anthropic's own changelog). Tie-in: directly relevant here — this repo's own CLAUDE.md is a very long, exhaustively detailed file; running `/doctor` against it would be a reasonable gut-check on whether any of it has drifted into "Claude could just read the code" territory.

**Headless mode has a `--json-schema` flag for schema-guaranteed output** *(New technique)*. Buried in the same changelog (a 2.1.205 bug fix for it existing), `claude -p --json-schema <schema>` forces `-p`/headless runs to return output validated against a JSON schema, instead of free text you have to regex/parse. Confidence: high, primary source. Tie-in: Council Loop, SOPForge, and Codex-verify all currently consume Claude's text output programmatically — this could replace ad hoc parsing in any of those with a guaranteed-shape response, similar to what the Workflow tool's own `schema` option does for subagents.

**A developer reverse-engineered hidden "steganographic" markers in Claude Code's system prompt** *(Claude buzz)*. The claim (an HN thread ~2 days old, plus several independent write-ups including the original thereallo.dev post) is that Claude Code subtly alters small text details (an apostrophe style, a date-separator character) in its system prompt when it detects a custom API base URL matching known reseller/gateway or Chinese-AI-lab domain lists, or a Chinese system timezone — apparently to fingerprint unauthorized resellers or model-distillation pipelines. Confidence: medium — consistent across multiple independent sources and has real HN traction, but there's no Anthropic confirmation and no direct source-code verification here. No tie-in forced; Brian's own setups use direct Anthropic keys, so this wouldn't trigger, but it's a notable trust/transparency story about a tool he uses daily.

Category 4 (Other-AI industry buzz): nothing newer than yesterday's digest turned up — GPT-5.6 (OpenAI) and Grok 4.5 (xAI) are still the latest from the major labs' own newsrooms, both already covered on 2026-07-12. DeepSeek/Meta/Mistral searches only surfaced generic leaderboard-aggregator pages with no clear new release date, so nothing verifiable to report there today.
