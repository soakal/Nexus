# Claude + AI Digest — 2026-08-25

## 1. `/usage` gets a per-loop breakdown; prompt cache TTL becomes configurable per-conversation vs. per-subagent [New capability]
Confirmed via the official CHANGELOG (v2.1.243), cross-checked against a secondary changelog aggregator (gradually.ai) — high confidence. `/usage` now shows a Loops section (run count, total tokens, tokens/run, last run) so a runaway or chatty loop is visible at a glance, not just inferred from total spend. The same release adds `promptCacheTtl`/`subagentPromptCacheTtl` settings letting you keep the main conversation on a 1-hour cache while subagents default to 5 minutes, plus org-level `modelPricing` settings for cost tracking and keyless sign-in via the Anthropic Console as a Bearer-key alternative. Tie-in: the per-loop `/usage` breakdown is a direct fit for Council Loop and any `/loop` runs — right now the only way to notice an autonomous loop burning tokens is total daily spend; this makes it visible per-loop while it's still running.

## 2. New-to-Brian techniques: nothing surfaced this cycle [New technique]
Simon Willison's site turned up nothing published since the already-known Bun/Rust and remote-control posts (both old). Hacker News results were the same stale set flagged in prior digests (the "everything the docs don't tell you" thread, general retrospectives) — nothing dated to this window. The direct r/ClaudeAI / r/ClaudeCode site-search again failed to surface actual thread content (a standing tooling gap on this specific query across multiple digest runs now, not a clean "nothing new" signal). Nothing cleared the bar today.

## 3. Anthropic had another global outage on Aug 24, part of a visible reliability pattern this month [Claude buzz]
Medium-high confidence — covered by multiple outlets same-day (Notebookcheck, CyberSecurityNews, Arabian Business, plus community "is Claude down" threads), not a single aggregator repeating one source. Claude.ai, the API, Claude Code, and Cowork all showed elevated errors/529s starting ~05:06 UTC Aug 24, affecting Opus 5, Fable 5, and others; Anthropic reported stabilizing by 08:30 UTC without disclosing root cause. Coverage frames it as one of several disruptions logged through August (Aug 5, 12, 13, 16, 18, 20, 24 cited). This is operational/reliability news rather than a viral demo, but the pattern has real secondary-press reach. Tie-in: worth having on the radar purely as awareness — NEXUS's router, scheduled jobs, and this digest routine itself all depend on Anthropic API availability; no action implied, just context if a NEXUS job fails mysteriously around one of these windows.

## 4. xAI shifts from chatbot to agent platform: Grok Bot (always-on AI teammates) and Grok Build (describe-an-app, it builds live) [Other AI]
Confirmed via multiple secondary outlets (Unite.AI, Reworked.co, aibase.com) describing the same launches consistently — primary xAI newsroom itself wasn't directly reachable via search today, so treat exact feature details as reported-not-primary-sourced. Grok Bot (beta, Aug 11) gives SuperGrok Heavy subscribers agents with their own cloud computer that sign into existing tools and finish multi-step jobs unsupervised; enterprise access is waitlisted. Grok Build exited early testing this month and is now available to all subscription tiers on web/iOS/Android — describe an app/game/dashboard and it builds live, with publishing, GitHub export, and connectors. Pure industry awareness (xAI competing directly with autonomous-agent and no-code-app positioning) — no forced project tie-in.
