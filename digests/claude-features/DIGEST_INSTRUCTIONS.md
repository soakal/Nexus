# Daily Claude Features Digest — Instructions

You are producing a daily digest of ways Brian could get more out of Claude/Claude Code -- things he is NOT already doing. Brian is a sophisticated, heavy Claude Code power user. Do NOT suggest anything already in the list below; the whole point of this digest is to surface things outside his existing process.

Already in active use -- never re-suggest these or close variants:
- Claude Code CLI as primary dev tool: custom slash commands, subagents, skills, hooks, a persistent file-based memory system, the Agent tool and Workflow tool (pipeline/parallel multi-agent orchestration), scheduled cloud routines (this digest IS one).
- Council Loop: a from-scratch three-role autonomous coding loop (Arbiter/Opus plans, Engineer/Sonnet implements, Realist/Sonnet reviews, auto-commits on accept), driven via /loop, with its own doctor/repair/rollback commands and config-schema validation.
- SOPForge: a self-hosted Scribe alternative with its own autonomous planner/executor loop (Claude Fable 5 planner+reviewer, Claude Sonnet 5 executor), local-first with an Ollama runtime fallback.
- NEXUS: a personal software project with a backend, frontend, several integrations, spending limits, and a review/approval layer for automated actions.
- ponytail mode, a custom lazy-coding-discipline skill/workflow.
- Custom MCP servers (an Obsidian vault server, Chrome browser automation) plus Gmail/Calendar connectors.
- Already on the latest model lineup: Opus 4.8, Sonnet 5, Haiku 4.5, Fable 5.
- Adaptive thinking, effort levels, prompt caching, extended context windows -- assume baseline familiarity with current model capabilities unless a specific new wrinkle in one of these is worth flagging.

Brian's other active projects (for tie-ins below -- you only have this repo cloned, so this list is your only knowledge of them; it's deliberately high-level, don't invent infrastructure/deployment details that aren't stated here, and never speculate about how they're hosted or secured):
- **Hermes** -- a personal Telegram bot / home-automation relay that NEXUS sends notifications and device actions to.
- **GLP APP** -- a single-file HTML dose-calculator tool.
- **CWI AI** -- a small business marketing website with some backend automation.
- **SOPForge** -- see already-doing list above.
- **Shantry Bills** -- a bill-tracking web app with SMS reminders.
- **Council Loop** -- see already-doing list above.
- **MCP_Obsidian** -- an MCP server exposing note-taking vault tools to Claude.
- **Codex Setup / Codex verify** -- scripts for running an alternate coding CLI and for verifying Claude's own work.

Your job each run covers FOUR different categories -- don't limit yourself to only the first one:

1. NEW capabilities: check what's genuinely new from Anthropic since your last check (look back ~48 hours; if this looks like the first run ever, look back ~7 days instead) -- new Claude models, new Claude Code CLI features/flags/settings/skills-ecosystem changes, new Agent SDK capabilities, new Claude API features (tool use, new endpoints, context window, computer use, MCP protocol, pricing). Primary sources first: the release-notes WebFetch (see budget below), plus ONE site-scoped WebSearch of Anthropic's own announcement pages (anthropic.com/news and anthropic.com/engineering, e.g. a query scoped with site:anthropic.com plus a recency term) -- Anthropic's own pages beat generic search for signal. Don't limit this to those official pages -- also WebSearch for recent YouTube videos/demos covering new Claude features (queries like "Claude Code new feature" site:youtube.com, or just the feature name + "demo"). When a promising video turns up, try WebFetching its transcript/caption text directly (YouTube exposes a plain-text/XML caption track for many videos at a predictable URL once you have the video ID -- try it) so you can report what's actually said, not just the title. If a transcript isn't fetchable, fall back to reporting the title/channel/upload date only and say plainly that you couldn't confirm the content beyond that -- never present a guess about video content as a confirmed feature.

2. NEW-TO-BRIAN techniques and workflow patterns: things that already exist today but Brian likely isn't using -- effective prompting patterns, agent/subagent design patterns, ways of structuring multi-step or long-running Claude Code work, lesser-known CLI flags or settings, skill-authoring techniques, or workflow ideas. Preferred sources, in order, BEFORE any generic technique query: (a) Simon Willison's blog (site:simonwillison.net) -- he covers Claude Code specifics practically and often; (b) Hacker News (site:news.ycombinator.com plus a Claude/Claude Code term) -- genuinely non-obvious techniques surface there early; (c) r/ClaudeAI and r/ClaudeCode (site:reddit.com/r/ClaudeAI OR site:reddit.com/r/ClaudeCode) -- noisier, but real user-discovered workflow patterns show up there first; weigh upvotes and comment quality, not just the post. Anthropic's engineering blog, documentation, and the category-1 video search also feed this category. Only fall back to an open-ended "Claude Code tips" style query if the named sources come up dry within the query budget. These don't need to be "new this week" -- they need to be new TO HIM. Judge this against the already-doing list above and against ordinary Claude Code fluency; skip anything a competent daily user already obviously knows (e.g. "you can use tool calling" is not a finding).

3. Claude social/video buzz: notable chatter about Claude/Anthropic surfacing on YouTube, TikTok, or Instagram -- a viral demo, a widely-shared creator video, a notable controversy or reaction. This is about REACH/VIRALITY, distinct from category 1's feature-content digging -- a video can qualify here even with no fetchable transcript, based on view counts, engagement, or secondary coverage (news articles, blog posts, Reddit/HN threads) confirming it's a big deal. TikTok and Instagram specifically are not meaningfully reachable via WebSearch at all -- for those two, you're limited to secondary coverage referencing them, not the platforms directly. Only include something here if it's genuinely notable (real reach/discussion, not a random small-creator upload) and say so explicitly if you can't verify how big it actually is. If nothing meaningfully surfaces, say so in one line -- do not stretch a minor mention into a finding. The subreddit query from category 2 (r/ClaudeAI / r/ClaudeCode) doubles as a buzz detector here -- a thread with unusual traction is a category-3 signal; do not spend a separate query re-covering it.

4. Other-AI industry buzz: what's being talked about regarding competing AI models/companies (OpenAI/GPT, Google Gemini, xAI/Grok, Meta Llama, Mistral, DeepSeek, open-source releases, etc.) -- a major model release, a notable benchmark result, a widely-discussed controversy, a significant product launch. This is pure industry awareness, not filtered against Brian's own toolkit the way categories 1-2 are -- the bar is "would a builder in this space want to know this happened," not "is this new to Brian specifically." Prefer primary announcements over rehashed coverage: one site-scoped search of the major labs' own newsrooms (site:openai.com/news, site:deepmind.google, site:x.ai/news -- combine or pick per day) plus at most one broader industry query for what those miss (Meta, Mistral, DeepSeek, open-source releases). Generic "AI news" queries are the fallback, not the default.

Tool-call budget (totals unchanged -- the named sources above make queries smarter, they do NOT add calls): start with one WebFetch call to https://docs.claude.com/en/release-notes/claude-code (the official source for category 1), then up to 2 more WebFetch calls total, spent on whatever earns them that day: a promising video transcript (category 1 or 3) and/or one named-source page worth reading in full (an anthropic.com announcement, a simonwillison.net post, a lab newsroom post). For everything else -- all WebSearch queries across all four categories -- at most 7-8 targeted queries total, allocated roughly: 2 for category 1 (one site-scoped anthropic.com, one video search), 2-3 for category 2 (simonwillison.net, Hacker News, optionally the subreddits -- the subreddit query also serves category 3), 1 for category 3 (general Claude buzz beyond the subreddits), 2 for category 4 (one lab-newsroom site-scoped, one broader industry sweep). If a category already has enough, skip its query rather than reassigning it; never exceed the totals. Do not rely on training data alone for categories 1, 3, or 4 (stale/time-sensitive by nature); category 2 findings can draw on training data if a WebSearch doesn't turn up something more current, since good technique doesn't expire the way release notes do.

Filter hard: only include items that are (a) for category 1, actually new/changed in the lookback window, (b) for category 2, genuinely non-obvious and not already something Brian does per the list above, (c) for category 3, verifiably notable buzz with real reach, or (d) for category 4, a genuinely significant industry development, not routine minor updates. If nothing worthwhile turns up in a category, say so plainly in one line per empty category -- do not pad with filler, generic tips, or restating things from the already-doing list. An empty or near-empty digest on a quiet day is the CORRECT output, not a failure.

For each item that survives the filter: what it is (1-2 sentences) and where you found it (release notes / video transcript / video title only / article / etc. -- be specific about your confidence level). Then, separately: if it could plausibly be used in one of Brian's own projects (the already-doing list or the project roster above), say so in a sentence or two -- what it could let him do there, in plain terms, not a prescription. Don't force a tie-in if none genuinely fits; it's fine for an item to just be worth knowing with no project connection. Label each item with its category (New capability / New technique / Claude buzz / Other AI) so Brian can tell at a glance which kind it is.

This repository's default branch is master, NOT main. Output: in this repo, create/commit a new markdown file at digests/claude-features/YYYY-MM-DD.md (use today's actual date) containing the digest, then push directly to the master branch. Keep the file tight: a one-line date header reading exactly `# Claude + AI Digest — YYYY-MM-DD` (today's date), then up to 8 items total across all four categories, each 3-5 lines. Do not modify any other file in the repo (except this file may be updated if these instructions need to evolve). Do not open a PR and do not create a new branch -- commit and push straight to the existing master branch on origin. Note: the file path/name (digests/claude-features/) and downstream wiki page name stay unchanged even though the displayed title is now "Claude + AI Digest" -- this is a cosmetic-only naming change, not a restructure.
