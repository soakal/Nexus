# Claude + AI Digest — 2026-07-19

**Claude Code 2.1.215 shipped** *(New capability, medium confidence — primary source, raw CHANGELOG.md, but several bullets overlapped word-for-word with yesterday's 2.1.214 entry, so treat the exact bullet list with some caution)*. The one clearly new, non-duplicated item: Claude no longer auto-invokes the `/verify` and `/code-review` skills on its own judgment — they now require an explicit invocation. Also listed: ISO timestamps on memory file metadata, expanded Docker permission prompts to cover daemon-redirect flags, a fix for streaming output truncation at exit in SDK/pipeline consumers, and several PowerShell/Remote Control/background-session bugfixes. Tie-in: directly relevant to Brian's own `verify` and `code-review` skills (both listed as available in this session) — any workflow or hook that relied on Claude self-triggering either one after a change now needs an explicit `/verify` or `/code-review` call instead.

**Category 2 (new-to-Brian techniques): nothing survived the filter this run.** Both the Simon Willison site search and the Hacker News search returned only older, already-circulated material (pricing-confusion posts, past "everything you can configure" and token-overhead threads already covered on 2026-07-18/07-14) — no fresh non-obvious technique this time. A combined r/ClaudeAI / r/ClaudeCode site-search also returned zero results.

**Category 3 (Claude social/video buzz): nothing verifiable this run.** A viral-video search turned up only routine "use Claude to grow your channel" content-marketing videos, not genuine buzz about Claude itself — nothing with confirmed reach or secondary press coverage surfaced.

**x.ai shipped "Automations" for Grok on July 16** *(Other AI, high confidence — primary source, x.ai/news)*. Automations are jobs Grok runs on its own on a schedule or triggered by an incoming email, alongside the same-week open-sourcing of Grok Build (xAI's own coding agent/TUI). Notable mainly as a competitor converging on the same "scheduled autonomous agent" territory this very routine already covers for Brian — no action implied, just a data point that the space is consolidating around the pattern.

**Gemini 3.5 Pro's rumored July 17 launch remains unconfirmed** *(Other AI, low confidence — only SEO-aggregator coverage, no Google/DeepMind primary source, and outlets disagree on whether it shipped or slipped again to August)*. Same unresolved story flagged on 2026-07-17; not repeating further detail since nothing has actually confirmed either way.
